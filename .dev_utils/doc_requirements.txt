# This file contains packages that are required for doing dev work with dvas.
# To install them, run:
# pip install -r dev_requirements.txt

# The following packages are required for compiling the docs from source
sphinx
sphinx-rtd-theme
plantweb
